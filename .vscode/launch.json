{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Dev",
      "request": "launch",
      "command": "yarn dev",
      "cwd": "${workspaceFolder}"
  }
  ]
}